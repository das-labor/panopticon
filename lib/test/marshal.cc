#include <gtest/gtest.h>

TEST(marshal,load)
{
	ASSERT_TRUE(false);
}

TEST(marshal,snaphot)
{
	ASSERT_TRUE(false);
}

TEST(marshal,save_load)
{
	ASSERT_TRUE(false);
}

TEST(marshal,missing_db)
{
	ASSERT_TRUE(false);
}
